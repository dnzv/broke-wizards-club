import React from 'react'

const Tag = ({ value }) => (
  <div style={{ fontSize: '0.58rem', borderColor: '#cbc6c3' }}
       className="font-bold uppercase mx-1 mb-1 px-2 py-1
       border-solid border shadow-inner">{value}</div>
)

export default Tag
